server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/patientdb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    org.springframework: INFO
